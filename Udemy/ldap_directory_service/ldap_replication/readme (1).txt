Note:

1. LDIF files will have the string "_ldif_file" appended to file name.
you have to save those files with .LDIF extension, before using it

2. LDIF files will have the string "_schema_file" appended to file name.you have to save those files with .SCHEMA extension, before using it
